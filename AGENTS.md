# Repository agent instructions

- After completing a requested change, always commit the completed work to the current branch.
- Run the relevant tests and checks before committing, and report the resulting commit hash.
- When the user explicitly asks to commit all changes, include the entire current repository working tree in that commit.
